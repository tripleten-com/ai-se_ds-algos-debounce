# Debounced Search Inputs

This is the starting code for debouncing lesson in TripleTen's AI Software Engineering program.

The solution snapshot (only the changed files) is found in the `solution-snapshot` directory.
