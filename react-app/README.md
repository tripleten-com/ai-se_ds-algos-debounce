# Debounce Search Inputs

This is the solution code for debouncing lesson in TripleTen's AI Software Engineering program.

The solution snapshot (only the changed files) is found in the `snapshot` directory.