export function linearSearch(_items: number[], _target: number): number {
  // TODO: check each item one by one
  // Return the index of the first match, or -1 if not found
  return -1;
}

export function binarySearch(_sortedItems: number[], _target: number): number {
  // TODO: implement binary search on a sorted array of numbers
  // Return the index of the match, or -1 if not found
  // Use <, >, and === to compare numbers
  return -1;
}